org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod identity
mod.id=lowball
mod.name=Lowball Uplink
mod.version=0.3.0
mod.group=dev.lowball

# Java 25 is required from Minecraft 26.1 onward.
mod.java=25

# --- Per-version dependency coordinates -------------------------------------
# Resolved from the Fabric maven; see build.gradle.kts for the per-version map.
deps.fabric_loader=0.19.5

# IntelliJ is not yet compatible with the configuration cache (fabric-loom#1349).
org.gradle.configuration-cache=false
